# no markdown in the repo except the README
*.md
!README.md

# build output (dist/ is committed on purpose — see PRD 5.3 — so it is NOT ignored)
build/
.cache/

# node
node_modules/
demo/dist/
*.tsbuildinfo

# emscripten
*.o
*.a
*.wasm.map

# editor / os
.DS_Store
.vscode/
.idea/
compile_commands.json
